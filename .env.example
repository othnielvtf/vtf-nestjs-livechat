# Server configuration
PORT=3000
NODE_ENV=development

# CORS settings
CORS_ORIGIN=http://localhost:3000

# Pusher-compatible authentication
PUSHER_APP_KEY=your-app-key
PUSHER_APP_SECRET=your-app-secret
